# Examples

这个目录放“怎么用 `shuxia-skill-library` 审 skill、改 skill、提炼 pattern”的示例。

建议优先放三类内容：

- `review-*.md`
  - 对某个 skill 的结构审查

- `rewrite-*.md`
  - 某个 skill 的改写前后对比

- `pattern-*.md`
  - 从外部 skill 提炼出的可迁移 pattern

目标不是堆案例，而是让别人可以直接照着学。
